
//INSERT IN STRING


let a1 = 'abcde';
let modifiedA1 = a1.slice(0, 3) + '-' + a1.slice(3);

console.log(modifiedA1); 
// In this example, a new string modifiedA1 is created with the character at index 3 inserted by '-'. The original string a1 remains unchanged.

